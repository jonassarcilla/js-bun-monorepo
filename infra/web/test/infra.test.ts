// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as AppRegistryStack from '../lib/app-registry-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/infra-stack.ts
test('SQS Queue Created', () => {
    // const app = new cdk.App();
    // const stack = new AppRegistryStack.AppRegistryStack(app, "TestAppRegistryStack", {
    //     clientName: "",
    //     environment: "dev",
    //     tags: {},
    //     attributes: {},
    //     env: {
    //         account: process.env.AWS_ACCOUNT!,
    //         region: process.env.AWS_REGION!
    //     },
    // });

    // const template = Template.fromStack(stack);

    // template.hasResourceProperties('AWS::SQS::Queue', {
    //     VisibilityTimeout: 300
    // });

    //     // WHEN
    //   const stack = new AppRegistryStack.AppRegistryStack(app, 'MyTestStack');
    //     // THEN
    //   const template = Template.fromStack(stack);

    //   template.hasResourceProperties('AWS::SQS::Queue', {
    //     VisibilityTimeout: 300
    //   });
});
